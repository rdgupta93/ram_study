package streamapi.comparable;

public class ComparableNotes {

    /*
      Comparable Interface :-
    The Comparable interface in Java is used to define the natural ordering of objects of a class
    compareTo(T o), which must be implemented by the class to define how objects are compared.

    int compareTo(T o);

    Returns:
            A negative integer if the current object is less than the specified object.
            Zero if the current object is equal to the specified object.
            A positive integer if the current object is greater than the specified object.
     */
}
